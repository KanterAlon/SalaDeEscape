@{
    ViewData["Title"] = "Habitación 2";
    string nombreJugador = ViewBag.NombreJugador as string;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <div class="Habitacion">
        <div class="habitacion-container">
            <h2>Acertijo Matemático</h2>
            <p>Para asegurar el despegue del vuelo, debes resolver el siguiente acertijo matemático. ¡Buena suerte, @nombreJugador!</p>
            <p>Pista: Si el vuelo A tiene 150 pasajeros y el vuelo B tiene el doble, ¿cuántos pasajeros tienen en total?</p>
            
            <!-- Formulario para descifrar la clave -->
            <form method="post" action="@Url.Action("Habitacion", "Home", new { sala = 5 })">
                <input type="text" name="clave" placeholder="Ingresa el total de pasajeros" required autocomplete="off">
                <button type="submit">VALIDAR</button>
            </form>

            @if (ViewBag.Error != null)
            {
                <div class="error">
                    <p>@ViewBag.Error</p>
                </div>
            }
        </div>
    </div>

    <script>
        function validateAnswer() {
            const answer = document.querySelector('input[name="clave"]').value;
            if (answer == 450) { // Suponiendo que 150 + 300 = 450 es la respuesta correcta
                window.location.href = '@Url.Action("Victoria", "Home")';
            } else {
                alert('Respuesta incorrecta. Intenta de nuevo.');
            }
        }
    </script>
</body>
</html>
